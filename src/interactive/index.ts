/**
 * Interactive Module - Web Server and Real-time Interaction
 *
 * Provides HTTP server for serving HTML/CSS/JS and WebSocket-like communication
 * for interactive geometry visualization with real-time constraint updates.
 *
 * Note: webapp.ts is run as a standalone server, not imported as a module.
 * To start the web server, run: node dist/webapp.js
 * Or from package.json: npm run dev
 */

// This module is primarily for documentation purposes
// The actual server is in webapp.ts which runs directly
